* Semaphore 信号量

  - Semaphore S 信号量

  - Operation P (荷兰语proberen 意为test)，有时也表达为wait

  - Operation V (荷兰语verhogen 意为increment)，有时表达为signal


   S >= 0时，表示资源是允许访问的。

   P原来减少S，V用来增加S


   信号量机制只用几个元素和简单操作，就解决了同步问题，因此它影响深远。
